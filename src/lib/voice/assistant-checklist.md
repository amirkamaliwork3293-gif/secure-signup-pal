# چک‌لیست تست دستی «دستیار هوشمند صوتی»

مسیر تست: هر صفحه‌ای که `Layout` دارد (به‌جز `/voice` و `/voice-products` که عمداً
دکمه‌ی شناور در آن‌ها نمایش داده نمی‌شود) → تپ روی آیکن ✨ گوشه‌ی پایین → شیت باز
می‌شود → یا دکمه‌ی میکروفون را بزنید یا «ورود دستی متن» را انتخاب کنید و جمله را
تایپ و «اجرای دستور» را بزنید (نتیجه‌ی هر دو مسیر دقیقاً یکی است، چون هر دو به
`parseAssistantCommand` می‌رسند).

پیش‌نیاز داده‌ای برای تست کامل: چند محصول (مثلاً «تیشرت مشکی» با قیمت خرید)، چند
مشتری، و چند فاکتور ثبت‌شده در تاریخچه.

| # | جمله‌ی ورودی | نیت تشخیص‌داده‌شده | نتیجه‌ی انتظاری |
|---|---|---|---|
| ۱ | «آقای شهریاری ۲۵۰ هزار تومان بدهکار است» | `customer_debt` (debtor) | مبلغ ۲۵۰٬۰۰۰، نام «شهریاری». تراکنش `customers.addTx` با type=`debt` (مانده مثبت = بدهکار). |
| ۱ب | «آقای شهریاری ۲۵۰ هزار تومان طلبکار است» | `customer_debt` (creditor) | همان مبلغ و نام، ولی type=`payment` تا مانده منفی شود و در بخش «طلبکاران» دیده شود. |
| ۱ج | «خانم کمالی ۲۵۰ هزار تومان را تسویه کرد» | `customer_debt` (settle) | پرداخت ۲۵۰٬۰۰۰ از ماندهٔ بدهکاری (یا اگر طلبکار بود، ثبت بدهی معادل برای صفر شدن). اگر مبلغ گفته نشود («آقای فلانی تسویه کرد») کل مانده صفر می‌شود. |
| ۲ | «ماهانه ۴۵ میلیون هزینه اجاره خانه است» | `expense` | `expenses.add` با `title: "اجاره خانه"`، `amount: 45000000`، `recurringDays: 30`، `at: Date.now()`. کارت سبز با متن «هزینه «اجاره خانه» به مبلغ ۴۵,۰۰۰,۰۰۰ تومان ثبت شد» + برچسب «تکرار ماهانه». در `/expenses` دیده شود. |
| ۳ | «تیشرت مشکی ویرایش قیمت ۴۵ هزار تومان» | `product_price_edit` | قیمت جدید ۴۵٬۰۰۰. اگر فقط یک «تیشرت مشکی» باشد → قیمتش تغییر می‌کند (کارت سبز). اگر چند مورد مشابه («تیشرت مشکی»، «تیشرت مشکی XL») → کارت انتخاب با دکمه‌ی هر کالا + دکمه‌ی «اعمال روی همه‌ی موارد مشابه». اگر جمله شامل «همه‌شون/هرچی» باشد، بدون پرسیدن روی همه اعمال می‌شود. |
| ۴ | «یادآوری پرداخت بدهی ساعت ۱۳:۳۰ تاریخ ۴/۴/۱۴۰۵» | `reminder` | `reminders.add({ title: "پرداخت بدهی", dueAt })` با تاریخ شمسی ۱۴۰۵/۰۴/۰۴ ساعت ۱۳:۳۰ (هم «۱۴۰۵/۴/۴» و هم «۴/۴/۱۴۰۵» و هم «چهار تیر ۱۴۰۵» درست خوانده می‌شود). کارت سبز با تاریخ شمسی خوانا. اگر ساعت گفته نشود → ۹:۰۰ صبح با ذکر «پیش‌فرض»؛ اگر تاریخ گفته نشود → امروز با ذکر «پیش‌فرض». «فردا/پس‌فردا/هفته دیگه» هم پشتیبانی می‌شود. |
| ۴ب | «هزینه اجاره خانه ۴۵ میلیون تاریخ ۱۵ فروردین» | `expense` + تاریخ | هزینه با `at` معادل ۱۵ فروردین سال جاری (یا سال گفته‌شده) ثبت شود، نه «اکنون». |
| ۵ | «پرسودترین کالای من چیه؟» | `query` → `most_profitable` | فقط خواندن: کارت 📊 با فهرست پرسودترین کالاها بر اساس `(قیمت فروش − قیمت خرید) × تعداد` از تاریخچه‌ی فاکتورها. اگر قیمت خرید ثبت نشده باشد، با ذکر همین نکته به «پرفروش‌ترین» برمی‌گردد. هیچ تغییری در داده ثبت نمی‌شود و دکمه‌ی تأییدی وجود ندارد. |
| ۶ | «بهترین مشتری من کیه؟» | `query` → `best_customers` | کارت 📊 با مشتریان مرتب بر اساس جمع مبلغ فاکتورها (و تعداد فاکتور). اگر هیچ مشتری ثبت‌شده‌ای فاکتور نداشته باشد، از نام مشتری روی خود فاکتورها جمع‌بندی می‌شود. بدون تغییر داده. |
| ۷ | «هوا امروز خیلی خوبه» (نامرتبط) | `unknown` | نباید به «کالایی پیدا نشد» بیفتد. کارت ⚠️ با راهنمای دستور یا سؤال + دکمه‌ی «ویرایش دستی». کنسول تمیز. |
| ۸ | «۲ تا نون» | `invoice_item` (fallback) | رفتار کاملاً مثل صفحه‌ی `/voice`: ۲ عدد «نون» به فاکتور جاری اضافه می‌شود (کارت سبز «به فاکتور اضافه شد»). اگر چند «نون» مشابه باشد → کارت انتخاب. این تست تضمین می‌کند لایه‌ی گزارش هیچ‌کدام از نیت‌های عملی را خراب نمی‌کند. |
| ۹ | «۱۵۰ عدد پیراهن با قیمت ۲۰۰ هزار تومان اضافه شود» | `product_add` | کالای جدید با نام «پیراهن»، موجودی ۱۵۰، قیمت ۲۰۰٬۰۰۰ با `products.save` اضافه شود (کد یکتا مثل ثبت صوتی محصولات). در `/products` دیده شود. |
| ۱۰ | «فاکتور آقای کمالی را باز کن» | `open_invoice` | مشتری «کمالی» پیدا شود و صفحهٔ `/invoices?q=کمالی` باز شود تا فاکتورهای همان مشتری فیلتر شوند. اگر چند هم‌نام → کارت انتخاب. اگر فاکتوری نبود → پیام «پیدا نشد» بدون کرش. |
| ۱۱ | «امروز چقدر سود داشتم» | `query` → `profit` (امروز) | کارت 📊 با سود امروز از همان فرمول `/reports`. اگر فاکتوری نبود: «امروز هنوز فاکتوری ثبت نشده…». بدون تغییر داده. |
| ۱۲ | «این ماه چقدر سود کردم» | `query` → `profit` (این ماه) | سود ماه جاری شمسی. «فروش امروز / این ماه چقدر فروختم» هم باید `sales` باشد نه کالا. |
| ۱۳ | «آقای کمالی چقدر بدهکاره» | `query` → `customer_status` | فقط خواندن مانده. نباید برود سراغ ثبت بدهی («مبلغ را نفهمیدم»). اگر مشتری نبود: «پیدا نشد». |
| ۱۴ | «چند تا بدهکار دارم» | `query` → `debtors` | همان خلاصه‌ی بدهکاران قبلی. «گزارش امروز» یک خلاصه‌ی فروش+سود+هزینه می‌دهد. |

## تست‌های ایمنی/خطا

- **رد دسترسی میکروفون:** دکمه‌ی میکروفون → رد اجازه → پیام زرد نمایش داده می‌شود و
  خودکار به حالت «ورود دستی متن» می‌رود (بدون کرش، بدون unhandled promise).
- **دستگاه بدون تشخیص گفتار** (`engine === "none"`): شیت مستقیم در حالت ورود دستی
  باز می‌شود.
- **مبلغ صفر/نامعتبر:** «آقای شهریاری بدهکار است» (بدون مبلغ) → کارت ⚠️ با راهنمای
  گفتن مبلغ؛ هیچ تراکنشی ثبت نمی‌شود.
- **چند دستور پشت‌سرهم:** بدون بستن شیت، دستور بعدی را بگویید — کارت جدید بالای
  کارت‌های قبلی اضافه می‌شود.
- **تداخل میکروفون:** شیت را ببندید؛ `rec.stop()` صدا زده می‌شود و شناساگر آزاد
  می‌شود. در صفحه‌های `/voice` و `/voice-products` دکمه‌ی شناور نمایش داده نمی‌شود.
- **بدون شبکه:** همه‌ی موارد بالا باید در حالت آفلاین هم کار کنند (این دستیار هیچ
  فراخوانی شبکه ندارد).
